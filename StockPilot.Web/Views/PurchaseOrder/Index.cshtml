@model List<StockPilot.EntityLayer.Entities.PurchaseOrder>
@using StockPilot.EntityLayer.Enums

@{
    ViewData["Title"] = "Purchase Orders";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="mb-1">Purchase Orders</h2>
            <p class="text-muted mb-0">Orders placed with suppliers.</p>
        </div>
        <a asp-controller="PurchaseOrder" asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle me-1"></i>New Purchase Order
        </a>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle me-2"></i>@TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-triangle me-2"></i>@TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (Model == null || !Model.Any())
    {
        <div class="card shadow-sm">
            <div class="card-body text-center py-5">
                <i class="bi bi-cart fs-1 text-muted"></i>
                <h5 class="mt-3">No purchase orders yet</h5>
                <p class="text-muted">Create your first purchase order to get started.</p>
                <a asp-controller="PurchaseOrder" asp-action="Create" class="btn btn-primary">
                    <i class="bi bi-plus-circle me-1"></i>New Purchase Order
                </a>
            </div>
        </div>
    }
    else
    {
        <div class="card shadow-sm">
            <div class="card-header"><strong>Order List</strong></div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">
                        <thead>
                            <tr>
                                <th>Order #</th>
                                <th>Date</th>
                                <th>Supplier</th>
                                <th>Warehouse</th>
                                <th class="text-end">Items</th>
                                <th class="text-end">Total</th>
                                <th>Status</th>
                                <th class="text-end">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var order in Model)
                            {
                                <tr>
                                    <td><strong>#@order.PurchaseOrderId</strong></td>
                                    <td class="text-muted small">@order.OrderDateUtc.ToString("yyyy-MM-dd HH:mm")</td>
                                    <td>@order.Supplier?.Name</td>
                                    <td>@order.Warehouse?.Name</td>
                                    <td class="text-end">@order.Items.Count</td>
                                    <td class="text-end fw-semibold">@order.TotalAmount.ToString("N2")</td>
                                    <td>
                                        @switch (order.Status)
                                        {
                                            case PurchaseOrderStatus.Pending:
                                                <span class="badge bg-warning text-dark">Pending</span>
                                                break;
                                            case PurchaseOrderStatus.Received:
                                                <span class="badge bg-success">Received</span>
                                                break;
                                            case PurchaseOrderStatus.Cancelled:
                                                <span class="badge bg-secondary">Cancelled</span>
                                                break;
                                        }
                                    </td>
                                    <td class="text-end">
                                        <a asp-controller="PurchaseOrder" asp-action="Details"
                                           asp-route-id="@order.PurchaseOrderId"
                                           class="btn btn-sm btn-outline-primary">
                                            <i class="bi bi-eye me-1"></i>Details
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
</div>